language: python
cache: pip

matrix:
  include:
  - os: linux
    dist: trusty
    python: '2.7'
  - os: linux
    dist: trusty
    python: '3.5'

script:
- pip install -e .
- ros-env create ~/ros/indigo

